import React from 'react';
import PropTypes from 'prop-types';

page.propTypes = {
    
};

function page(props :any) {
    return (
        <div className='mt-[90px]'>
            sample page
        </div>
    );
}

export default page;